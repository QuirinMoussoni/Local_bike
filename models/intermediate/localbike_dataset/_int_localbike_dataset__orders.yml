version: 2

models:  
  - name: int_localbike_dataset__orders
    description: "This model must contain relevant info about orders details infos"
    columns:
      - name: order_id
        description: "Primary key, unique identifier for each order."
        tests:
          - unique
          - not_null
